<!DOCTYPE html>
<html>
<head>
    <title>Bingo Web</title>
    <meta charset="UTF-8">
</head>

<style>
body {
    margin: 0;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
}

.tabla {
    margin: 3em;
    display: flex;
    flex-direction: column;
}

.fila {
    display: flex;
}

.fila > div {
    border: 1px solid #001f3f;
    width: 5em;
    height: 5em;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3em;
    text-align: center;
}

.celda {
    background: #007C9C;
    color: black;
}

.vacia {
    background: #000000;
}
</style>

<body>
    <div class="tabla">
        {% for fila in carton %}
            <div class="fila">
                {% for celda in fila %}
                    {% if celda == 0 %}
                        <div class="vacia">
                        </div>
                    {% else %}
                        <div class="celda">
                            {{ celda }}
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        {% endfor %}
    </div>
</body>
</html>